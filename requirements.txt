# Runtime dependencies. Install with:  pip install -r requirements.txt
# (The current proof-of-concept scrapers use only the Python standard library,
#  so nothing here is required yet — these are for the next stage.)
requests>=2.31
