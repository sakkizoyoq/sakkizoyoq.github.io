# Зависимости. Установка:  pip install -r requirements.txt
#
# Сборщики цен обходятся стандартной библиотекой Python — ничего ставить
# не нужно. Pillow требуется только сборке страницы: он уменьшает картинки
# товаров, иначе страница распухает до десятков мегабайт.
Pillow>=10.0
